<section class="prose">
  <h1 class="prose__title">{{ page.title }}</h1>
  <div class="prose__body">{{ page.content }}</div>
</section>

{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
